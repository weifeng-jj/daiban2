<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.reverseclock">

    <!-- 必要权限 -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- 定位和网络权限（用于获取当前位置和天气信息） -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ReverseClock">

        <!-- 主界面：底部导航（闹钟/秒表/计时器） -->
        <activity
            android:name=".MainNavActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 创建/设置闹钟页面 -->
        <activity
            android:name=".MainActivity"
            android:exported="false"/>

        <!-- 关闹钟页面 -->
        <activity android:name=".MathQuestionActivity" android:exported="false"/>
        <activity android:name=".ShakeActivity" android:exported="false"/>
        <activity android:name=".PuzzleActivity" android:exported="false"/>
        
        <!-- 待办闹钟页面 -->
        <activity 
            android:name=".TodoAlarmActivity" 
            android:exported="false"
            android:showOnLockScreen="true"
            android:turnScreenOn="true"/>

        <!-- 闹钟接收器 -->
        <receiver android:name=".AlarmReceiver" android:exported="false"/>
        
        <!-- 待办提醒接收器 -->
        <receiver android:name=".TodoAlarmReceiver" android:exported="false"/>

    </application>

</manifest>